function MainButtons(props) {
    return(<div className='mainButtons'>
        <button onClick={handleWhereIsClick}>WHERE IS MY COAT?</button>
        <button onClick={handleWhosThatClick}>WHOS THIS COAT?</button>
    </div>)
}

export default MainButtons